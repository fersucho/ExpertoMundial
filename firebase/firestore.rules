rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Acceso directo de clientes denegado.
      // Toda la lectura y escritura se realiza de forma segura mediante Firebase Admin SDK en las Cloud Functions.
      allow read, write: if false;
    }
  }
}
